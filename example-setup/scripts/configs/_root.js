"use strict";
// Just a simple way to configure the projects root path in all configuration backup scripts.
// As well as keeping those scripts clean for quick readability if you need to come back and update something.
module.exports = require("path").join(__dirname, '..', '..');